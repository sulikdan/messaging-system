import { ProductModelModel } from './product-model.model';

describe('ProductModelModel', () => {
  it('should create an instance', () => {
    expect(new ProductModelModel()).toBeTruthy();
  });
});
